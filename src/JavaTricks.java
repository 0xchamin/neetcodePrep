import java.util.HashMap;

public class JavaTricks {
    public static void main(String[] args) {

        // Access keys in a map
        HashMap<String,Integer> myTestMap = new HashMap<>();

        //to insert new value
        // --> map.put(key, value)

        // to access value by key
        // map.get(key)

        // to access value by key with default
        // map.getOrDefault(key, defaultValue).

    }
}
